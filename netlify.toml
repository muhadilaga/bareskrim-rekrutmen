[build]
  command = "npm run build"
  publish = ".next"

# Jadwalkan fungsi cron untuk retry laporan Discord yang gagal
# Setiap 5 menit
[[scheduled_functions]]
  name = "discord-retry"
  interval = "*/5 * * * *"

[functions]
  included_files = ["src/app/api/cron/**"]
